//Vilius Minkevicius PS 1 kursas, 2 grupe

#include <stdlib.h>
#include <malloc.h>
#include "eile.h"

int main()
{
	int d;
	printf("Testukas. \n");
	scanf("%d", &d);
	int i, j;
	eil eilInt = NULL;
	kurti_eile(&eilInt);
	for(j = 0; j < 3000; j++) {
		for(i = 0; i < 100; i++)
		{
			iterpti(&eilInt, i);
			//printf("%d ", i);
		}
		for(i = 0; i < 93; i++)isimti_elementa(eilInt, NULL);
	}

	/*kiekviena cikla eiles dydis padideja 7 */
	printf("eiles dydis yra %d \n", eiles_dydis(eilInt));
	istrinti_eile(&eilInt);
	return 0;
}
